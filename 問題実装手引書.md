# 駿台2026年実戦問題集（英語リーディング）実装手引書

> **対象読者**: `uni-ent-exeam-engl` で駿台各回の大問1〜8を ReadLens-Pro 互換の `data.json` に落とし込む作業、および **ReadLens-Pro リポジトリ内**で Z会模試（`data/zkai/2026/`）を直接整備する作業を行う **すべての作業者（人間・AIエージェント）** 向け。  
> **目的**: 1人の作業者の暗黙知を完全に明文化し、別の AI が途中から引き継いでも同じ品質で続行できるようにすること。  
> **基本姿勢**: 「学習者が解説冊子を読み上げているのと同等の体験」を Web ビューア上で再現する。冊子に書かれていない情報を勝手に加えない、しかし学習者の理解を助ける補足は専用フィールドに切り出して控えめに添える。

**配置**: 本ファイルは **`ReadLens-Pro/問題実装手引書.md`** に置く。駿台パイプライン（`uni-ent-exeam-engl`）と **同一リポジトリ内の Z会データ**（`ReadLens-Pro/data/zkai/2026/`）の両方を参照する。Z会専用の詳細は **§0.1**。

---

## 0. 前提となるリポジトリ構成

```
uni-ent-exeam-engl/                         ← このリポジトリ（データ生成側）
├─ data/sundai-2026-reading/
│   ├─ round-01/
│   │   ├─ manifest.json                    ← 抽出マニフェスト
│   │   └─ sections/q01.json … q08.json     ← PDF から自動抽出した素データ
│   ├─ round-02/ … round-05/
│   └─ vocab/round-01.json …                ← 「主な語句・表現」をラウンド単位でマージしたもの
├─ images/sundai-2026-reading/round-01/
│   ├─ q01/ q02/ q05/ q06/ q07/ q08/        ← 各大問の図版PNG（人手で切り出して保存）
│   └─ ...
└─ tools/
    ├─ extract_sundai_round.py              ← PDF → q01〜q08.json
    ├─ build_sundai_vocab_json.py           ← 「主な語句・表現」を集約
    ├─ inspect_pdf.py / sundai_reading_pdf_root.py
    ├─ register_qNN_assets.py               ← 各大問の figures を q.json に登録
    ├─ build_readlens_2026_round01.py       ← ★ メインビルダー（→ ReadLens-Pro/data.json）
    ├─ _section07.py / _section08.py        ← 大問7・8の専用ビルダー
    └─ __pycache__/
```

```
ReadLens-Pro/                                ← 同階層に存在する Web ビューア側
└─ data/sundai/2026/round01/
    ├─ data.json                             ← ★ 最終成果物
    └─ images/q01_*.png q02_*.png …          ← ビルド時にコピーされる
```

> **重要**: 元 PDF は Google ドライブ側の  
> `G:\マイドライブ\本のデータベース\bookshelf\(駿台)2026年駿台実戦問題集_英語リーディング\第N回_問題.pdf`  
> および `第N回_解説.pdf` を `tools/sundai_reading_pdf_root.py` 経由で参照している。OCR/抽出ミスは PDF 由来であり、絶対に「PDF が壊れている」と先に決めつけない。

---

## 0.1 ReadLens-Pro 内の「Z会 共通テスト実戦模試」ライン（`data/zkai/`）

駿台用の `uni-ent-exeam-engl` 抽出パイプラインを経由せず、**ReadLens-Pro 単体**で Z会模試を実装する場合の慣例。第5回などで運用済み。

### ディレクトリ

```
ReadLens-Pro/
└─ data/zkai/2026/roundNN/
   ├─ data.json                    ← 最終成果物（sections に大問1〜を配列で格納）
   ├─ build_section01.py …         ← 大問ごとに分割可。main() で当該 section を差し替えマージ
   ├─ images/                      ← グラフPNG等（相対パスは data.json からの相対で記述することが多い）
   └─ …
```

### ビルドの流れ

1. `build_sectionMM.py` に `section_MM()`（または同等の dict）を定義する。
2. `main()` で `data.json` を読み込み、`section_number == MM` の要素を除去してから `sections.append(...)`、`implemented_sections` に `MM` を追加して保存する（**第5回・大問7・8** が参照実装: `build_section07.py`, `build_section08.py`）。
3. グラフが必要な場合は **matplotlib** で `images/` に PNG を生成し、`passage.graph_image` または `question.figure_image` の `src` で参照する。

### `exam_info`

- `implemented_sections`: 実装済みの大問番号のソート済み配列（例: `[1,2,…,8]`）。
- `quoted_source` / 解説の出典表記は **`Z会『2026年 共通テスト実戦模試 英語リーディング』第N回 解説冊子`** 等、出版社に合わせてよい（§4.1 の駿台固定文と同列の「逐語＋出典明示」ポリシーを踏襲する）。

### ビューア側の追加仕様（本リポジトリの `viewer.js`）

- **`figure_image`**: `caption_ja` に加え **`caption_en`** も表示可能（図下の補足・選択肢とパネルの対応など）。
- **設問 `stem` の単一ブロック**（`stem.en` / `stem.ja` に `\n` を含む場合）: 改行は **`<br>` に変換されて表示**される。
- **4パネル図＋空テキスト選択肢**（例: パーム油・どのグラフか）: 問題冊子と同じく **左上=① … 右下=④** と明示し、画像側にもパネル番号を重ねると学習者が迷わない（`build_section07.py` 参照）。

---

## 1. 全体ワークフロー（高レベル）

```
[原典PDF]
   │  (a) tools/extract_sundai_round.py
   ▼
[data/.../round-NN/sections/qXX.json]   ← OCR崩れを含む生テキスト
   │  (b) Cursor 等で図版を切り出し → images/.../qXX/ に保存
   │  (c) tools/register_qXX_assets.py で figures を qXX.json に登録
   ▼
[qXX.json + figures + 解説冊子]
   │  (d) tools/build_readlens_2026_round01.py の SECTION_X を執筆／更新
   │      - quoted_ja は解説冊子から逐語転載
   │      - instructor_note に学習者向け補足
   │      - evidence_sentences で本文文IDをリンク
   ▼
[ReadLens-Pro/data/sundai/2026/round01/data.json]  ← 最終成果物
   │  (e) verify*.txt 系で正解番号・evidence の整合性を確認
   ▼
[Web ビューア（ReadLens-Pro/viewer.html）で確認]
```

各ステップの詳細は §3 以降。  
**1ラウンド分の標準工数**: 大問1〜8まで通すと、語彙整理含めおおむね丸1日。

---

## 2. ★ 着手前に必ず行う「出題形式の検証」

> **このセクションが本手引書で最も重要**。ラウンド01の8大問は確かに典型的な共通テスト型に揃っているが、**ラウンド02以降で出題形式が完全に同じとは限らない**。  
> 各ラウンド着手時に、必ず以下のチェックリストを通してから本実装に入ること。検証結果は `_verify_round-NN.txt` 等に書き出して残す。

### 2.1 ラウンド着手時のチェックリスト（10分作業）

1. `data/sundai-2026-reading/round-NN/manifest.json` を開き、`problemSectionsFound` と `explanationSectionsFound` がともに `[1,2,3,4,5,6,7,8]` であることを確認。欠けている大問があれば、PDF抽出を疑い `tools/extract_sundai_round.py` を再実行。
2. 各 `sections/qXX.json` の `problem.text` を1ファイルずつ流し読みし、以下を確認:
   - **配点**: 「(配点 N)」の数値（後述の §4 と差があれば異常）
   - **小問数**: 「問1」「問2」「問N」のカウント
   - **設問形式**: 4択／5択／順序整列／表完成／グラフ読み取り／ペア選択（順不同）等
   - **本文の媒体**: パンフレット／物語／メール／エッセイ草稿／記事＋プレゼン概要／レポート＋情報源 等
   - **画像の有無**: 挿絵・選択肢が画像のもの（特に大問1・3・6）／図表・グラフ（大問7・8）
3. `explanation.text` 側で:
   - 「(主な語句・表現)」ブロックの存在を確認
   - 「設問解説」または「I設問解説I」の見出し（OCR崩れに注意）の存在を確認
   - 全訳の段落分割が、問題本文の段落数と対応しているかを確認
4. 上記から「本ラウンドの大問X は 大問X(round-01) と同じ型である」または「異なる」を `_verify_round-NN.txt` に明記。**異なる場合は、実装の前に `SECTION_X` の構造を見直す方針を立てる**（=その大問用に新しい builder 関数を起こす可能性がある）。

### 2.2 「同じ型」と判定した場合の流れ

- `tools/build_readlens_2026_round01.py` をテンプレートとしてコピー → `build_readlens_2026_round02.py` を作成
- 出力先パスを `ReadLens-Pro/data/sundai/2026/round02/` に書き換え
- 各 `SECTION_X` の中身（本文・選択肢・解説の文字列）だけを差し替える
- フィールド構造（`passages`, `paragraphs`, `questions`, `explanation.instructor_note` …）はそのまま流用

### 2.3 「型が違う」と判定した場合の流れ

- 該当大問のために `tools/_sectionXX_round-NN.py` のような専用ビルダーを新設（大問7・8 と同様の方式）
- 既存の type を踏襲しつつ、ビューア（`ReadLens-Pro/viewer.js`）が解釈できる範囲で fields を拡張
- ビューア側に新フィールドが必要な場合は、別作業者（フロント担当）への相談メモを `_handoff_round-NN_qXX.md` として残す

---

## 3. 大問別 出題形式リファレンス（round-01 検証済み）

> 各大問について「典型的な構造」「答え方」「使うフィールド」を一望できるようにまとめた。round-02 以降で食い違いが出たら、その箇所だけ §2 の手順で再検証すること。

### 3.1 大問1（配点6・3問・各2点）— 短文読解（パンフレット型）

- **本文**: A4 1〜2ページ程度のパンフレット。挿絵が3点ほど右側に float で配置される。
- **設問**: 全3問4択。ただし**問3は「絵で答える4択」**（選択肢が4枚の挿絵）。
- **JSON 上の特徴**:
  - 各 passage に `image: { src, alt, float: "right" }` を持たせる
  - 問3 の question に `figure_image: { src, alt }` を設定し、各 choice の `en/ja` を空文字にして「画像から正解番号だけを取る形」にする
  - `evidence_sentences` は本文中の文ID（例: `wc_s3`）を配列で
  - 例: `data/sundai/2026/round01/data.json` 第1問

### 3.2 大問2（配点10・4問）— 説明的文章＋図1点＋短い意見表

- **本文**: 中程度の説明文＋簡単な図。
- **設問**: 4問。意図確認・段落要旨・対比など。
- **JSON 上の特徴**: `passages` は1〜2ブロック、`questions` は4問。挿絵 1〜2 点。

### 3.3 大問3（配点9・3問・各3点）— 物語文（短編ストーリー）

- **本文**: 1〜2ページの物語。会話多め。
- **設問の典型**:
  - 問1: 「この物語の語り手は誰か」（4択）
  - 問2: **順序整列問題**（5つから4つを選び、起こった順に並べる）
  - 問3: 結末の感情を選ぶ系（4択）
- **JSON 上の特徴**:
  - `passages[].paragraphs` を **段落の配列の配列** にする（外側=段落、内側=文）
  - 順序整列の問題には `answer_numbers: [N1, N2, N3, N4]` と `answer: { "N1": "③", "N2": "⑤", … }` を用いる
  - 採点規則「全部正解で3点」を `answer_note: "全部正解で3点"` で明記

### 3.4 大問4（配点12・4問・各3点）— エッセイ添削（草稿への教師コメント）

- **本文**: 学生のエッセイ草稿。本文中に `(1)∧` `(2)∧` のようなキャレット記号が挿入される。
- **設問**: 教師コメントに従って**段落の途中に文を挿入**したり、**置換**する4択問題が中心。
- **JSON 上の特徴**:
  - 文オブジェクトに `comment_marker: "(1)"`, `marker_position: "before"`, `marker_type: "caret"` を持たせる
  - 教師コメントは `passages` の末尾に独立 passage として、または `margin_comments` 配列として表現
  - ビューアは `comment_marker` を見て、該当文のキャレット記号を本文中に表示する

> **★ レイアウト崩壊が頻発する大問のため、§3.4.1 で『round-01 大問4 を金字塔（gold standard）とする厳密再現プロンプト』を別建てで定義している**。他AIに委任する場合・自分で実装する場合の両方で、§3.4.1 を必ず読むこと。

---

### 3.4.1 ★★ 大問4 厳密レイアウト再現プロンプト（round-01 を gold standard）

> **背景**: 大問4 は「PDF の左右2段組（左＝草稿本文／右＝教師コメント）」「文中の `(N)∧` キャレット」「下線部の波線」「末尾の Overall Comments の colspan=2 帯」など視覚要素が多く、**他AIに渡すと最も再現が崩れやすい大問**である。round-01 では `tools/build_readlens_2026_round01.py` の `SECTION_4`（行 1239〜1666）が完成形として機能しており、これを**逐字レベルの参照実装**として扱う。新ラウンドでも、まずこの構造を1文字も崩さずに移植してから、本文・コメント文字列を差し替えるのが鉄則。

#### A. PDF 原典の視覚要素 → JSON フィールドの対応表

`data/sundai-2026-reading/round-01/sections/q04.json` の `problem.text` を読むと、PDF 原典の各視覚要素が以下のように落とし込まれていることが分かる:

| PDF 上の視覚要素 | JSON 上の表現 | ビューア（`viewer.js`）での描画 |
|---|---|---|
| 草稿のタイトル「The Benefits of Volunteering」と「Comments」の **2段組ヘッダ** | `passage.title.{en,ja}` ＋ `passage.margin_comments[]`（中身が1件以上ある事実） | `<table class="essay-table">` の `<thead>` 自動生成（左70% / 右30%） |
| 草稿本文（左カラム） | `passage.paragraphs[][]`（配列の配列） | 各段落が `essay-cell-main` の `<tr>` 1行 |
| **`(1)∧` `(2)∧` `(3)∧` `(4)∧` の文中キャレット** | 該当文オブジェクトに `comment_marker: "(N)"`, `marker_position: "before"`, `marker_type: "caret"` | `<sup class="comment-marker">(1)</sup><span class="caret-mark">∧</span>` で文先頭に挿入 |
| マーカー位置が **段落末尾の文の手前** か **段落冒頭** か **文中インライン** か | `marker_position: "before"` を使い、文オブジェクトを **マーカー直後の英文単位で分割**して配置 | `marker_position` が `"before"` ならその文の先頭に、`"after"` なら末尾に表示 |
| **下線部** (`(3)have a nice time` / `(4)have a lovely time with friends`) | 該当文に `underline_word: "have a nice time"`（**en の中の連続文字列を完全一致で指定**） | `<span class="underline-word">…</span>` で波線下線 |
| 右カラムの **欄外コメント (1)〜(4)** | `passage.margin_comments[] = [{marker, en, ja}, …]` | `<td class="essay-cell-comments">` に該当段落のコメントだけ列挙（`marker` で id マッチ） |
| 末尾の **Overall Comments**（colspan=2 の帯） | `passage.teacher_comment: {en, ja}` | `<tr><td colspan="2" class="essay-teacher-comment">` で全幅表示 |
| 各設問の **「下線部に代わる最もよい表現」** | `questions[].stem` のみで OK（下線部自体は本文 passage の `underline_word` で既に視覚表現済み） | — |

#### B. 文の分割ルール（最重要・崩れの主因）

PDF 原典では1文の途中にキャレットや下線が入るため、**JSON では「マーカーや下線の単位で文オブジェクトを分割し、id サフィックスで `_a` `_b` などに分ける」**のが round-01 の流儀。以下を**機械的に守る**:

1. **マーカー `(N)∧` が文頭にある場合** → その文1つを `comment_marker: "(N)"`, `marker_position: "before"`, `marker_type: "caret"` で表現（例: `ve_s7`）。
2. **マーカー `(N)∧` が段落の途中の文の前にある場合** → その文1つに同じ3フィールドを付ける（例: `ve_s6`）。**前の文と結合してはいけない**（しないとマーカーが行の真ん中に出ない）。
3. **下線部が文の一部分にある場合** → 文をその位置で2つに割る。
   - `..._a`: 下線の手前まで（`ja` も同じ位置で割る）
   - `..._b`: 下線の単語と、それ以降を含む。`underline_word` には **`_b` の `en` の中に完全一致で含まれる連続部分文字列**を入れる。
   - 例（round-01 第5段落）:
     ```jsonc
     { "id": "ve_s13a", "en": "In summary, through volunteering, students can",
       "ja": "まとめると，ボランティア活動を通して学生は" },
     { "id": "ve_s13b", "en": "have a nice time.",
       "ja": "(3)すてきな時間を過ごすことができる。",
       "comment_marker": "(3)", "marker_position": "before",
       "underline_word": "have a nice time" },          // ← _b.en に完全一致
     ```
   - **間違いの典型**: `underline_word` を `"have a nice time."` （ピリオド込み）にしてしまうと `String.replace` が一致せず下線が出ない／`"have a nice time. They can develop ..."` のように長く取りすぎて次文を巻き込む。
4. **マーカーと下線が同じ文に同居する場合**（例: 第5段落 `ve_s13b`, `ve_s14b`）: `comment_marker`, `marker_position`, `underline_word` を**同じ文オブジェクトに同時に書く**。`marker_type` は省略可（カラットなし数字だけならそれで OK）。

#### C. `margin_comments` と `teacher_comment` の必須事項

- `margin_comments` の各要素は **`marker` キーが必須**（`comment_marker` と完全一致する文字列）。  
  ビューアは `passage.margin_comments.find(c => c.marker === sent.comment_marker)` で結びつけるため、**マーカー文字列がズレると右カラムにコメントが出ない**。
- `margin_comments` の `en` と `ja` は両方必須。`ja` を空にすると右カラムの和訳が空白行になる。
- `teacher_comment` は `passages[0]` 直下のキー（`margin_comments` の中ではない）。位置を間違えると colspan=2 の Overall 帯が出ない。
- **数: 通常は4個**（`(1)〜(4)`）。問数と完全一致させる。

#### D. 他AI委任用 必須プロンプト（コピペ・一字も削らない）

```text
=== 大問4（エッセイ添削）厳密レイアウト再現ルール（絶対遵守）===

参照する gold standard 実装:
  uni-ent-exeam-engl/tools/build_readlens_2026_round01.py の SECTION_4（行 1239〜1666）
  uni-ent-exeam-engl/問題実装手引書.md §3.4.1（A〜F）

提出物: tools/build_readlens_2026_roundNN.py の SECTION_4 ブロック1個

★ 構造規則（違反は全件やり直し）

(1) passages は 1 個のみ。passage.id は "volunteer_essay" など意味のある英小文字スネーク。
    title.{en,ja} は草稿のタイトル（PDF 上段の左カラム見出し）。

(2) passage.paragraphs は 段落の配列の配列（外側=段落、内側=文オブジェクト）。
    PDF の段落分けと完全一致させる（round-01 では 5 段落＝外側配列の長さ 5）。

(3) 各文オブジェクトは {id, en, ja} を必ず持つ。id は "<prefix>_s<連番>" 形式。
    例: "ve_s1", "ve_s2", ...

(4) 文中にキャレット (N)∧ がある場合:
    - その文 1 つに以下の 3 フィールドを必ず追加（順不同）:
        "comment_marker": "(N)",
        "marker_position": "before",   // PDF で文頭にある場合。文末なら "after"
        "marker_type": "caret"          // (N)∧ のように山型を出す場合に必須
    - キャレット無しの数字だけ（インラインの (3) 等）の場合は marker_type を省略
    - 前の文と結合しない（必ず単独の文オブジェクトとして残す）

(5) 下線部がある場合:
    - 下線の単位で文オブジェクトを 2 分割し、id を "<base>_a" "<base>_b" にする
    - "_b" 側に "underline_word": "<下線単語列・英語のまま・完全一致>" を追加
    - underline_word は "_b".en の中に「ピリオド・カンマを除いた連続文字列」として実在しなければならない
    - "_a" 側の en は ".  " などの末尾句読点を含めない（自然な英文の途中で切る）

(6) passage.margin_comments は配列で、各要素は {marker, en, ja} の 3 キー必須。
    marker は文中の comment_marker と完全一致する文字列。
    数は通常 4 個（(1)〜(4)）で、問数と一致させる。

(7) passage.teacher_comment = {en, ja} を passages[0] 直下に置く（margin_comments の中ではない）。
    PDF 末尾の Overall Comments を逐語転載する（顔文字 ☺ を含めて）。

(8) questions は通常 4 問（問1〜問4）、各 3 点。answer_number は通常 14〜17 連番（roundでズレることあり、PDF を見て確定）。

★ 検証手順（提出前に必ず自分で実行）

A) 各段落について、PDF の (N)∧ の位置と JSON の comment_marker 付き文の位置が一致するか
   目視チェック（→ 違ったら必ず修正）

B) 各 underline_word について、対応する _b.en の中に
   完全一致（大文字小文字含めて）の連続部分文字列として実在することを Python で確認:
     for p in passage["paragraphs"]:
       for s in p:
         if "underline_word" in s:
           assert s["underline_word"] in s["en"], (s["id"], s["underline_word"], s["en"])

C) margin_comments の marker 全件が、本文中の comment_marker のどれかに対応することを確認:
     mc_set = {mc["marker"] for mc in passage["margin_comments"]}
     cm_set = {s["comment_marker"] for p in passage["paragraphs"]
                                    for s in p if "comment_marker" in s}
     assert mc_set == cm_set, (mc_set ^ cm_set)

D) teacher_comment が passages[0] 直下に存在し、{en, ja} を持つことを確認

E) ブラウザで http://localhost:8000/viewer.html?data=data/sundai/2026/roundNN/data.json を開き、
   大問4 ページで以下が PDF と同じに見えることを目視確認:
   - 左右2列の essay-table（左の見出し = 草稿タイトル, 右の見出し = "Comments"）
   - 各段落の右カラムに、その段落で参照されている (N) コメントだけが表示される
   - (1)∧ 〜 (4)∧ のキャレットマークが正しい文の先頭に出る
   - (3) と (4) の下線部に波線が引かれている
   - 最下段に colspan=2 で Overall Comments の帯が出る

★ 禁止事項（過去に発生した失敗パターン）

× passage を 2 個（本文用とコメント用）に分けること
   → margin_comments は本文 passage の中に配列として持たせるのが正解。
× margin_comments の各要素を {marker, comment, ja} のように勝手なキー名にすること
   → ビューアは {marker, en, ja} を期待。`comment` キーでは右カラムが空になる。
× underline_word に "have a nice time." とピリオド込みで書くこと
   → String.replace が一致せず下線が出ない。
× キャレット位置の文を前後の文と結合して 1 文にまとめること
   → マーカーが行の中央に出てしまう／コメントとの結びつきが切れる。
× teacher_comment を margin_comments の最後の要素として混ぜ込むこと
   → colspan=2 の Overall 帯が描画されない。
× ja 訳に注釈 (3) (4) のような番号を入れること、または抜くことを統一しない
   → round-01 では ja の中にも "(3)すてきな時間〜" のように番号を残しているので合わせる。

=== ここまで ===
```

#### E. 受領後の自動検証（人間／親エージェント側）

`§4.2.1 D` の検証スクリプトに、大問4 専用の以下チェックを **必ず追加**:

```python
import re

def verify_section4(sec: dict) -> list[str]:
    bad: list[str] = []
    if sec.get("section_number") != 4:
        return bad
    if len(sec.get("passages", [])) != 1:
        bad.append("大問4: passages は 1 個のみであるべき（本文＋コメントは同一 passage 内）")
    p = sec["passages"][0]

    # margin_comments のキー名チェック
    mcs = p.get("margin_comments") or []
    if not mcs:
        bad.append("大問4: margin_comments が空")
    for i, mc in enumerate(mcs):
        if set(mc.keys()) - {"marker", "en", "ja"}:
            bad.append(f"大問4: margin_comments[{i}] に余計なキー {set(mc.keys()) - {'marker','en','ja'}}")
        for k in ("marker", "en", "ja"):
            if not mc.get(k):
                bad.append(f"大問4: margin_comments[{i}].{k} が空または欠落")

    # 本文側の comment_marker と margin_comments.marker が完全一致するか
    cm_in_text: set[str] = set()
    for para in p.get("paragraphs", []):
        for s in para:
            if "comment_marker" in s:
                cm_in_text.add(s["comment_marker"])
                # marker_position が "before" / "after" のいずれか
                if s.get("marker_position") not in ("before", "after"):
                    bad.append(f"大問4: {s['id']} の marker_position が不正: {s.get('marker_position')}")
    mc_set = {mc["marker"] for mc in mcs}
    if cm_in_text != mc_set:
        bad.append(f"大問4: comment_marker 集合と margin_comments.marker 集合が一致しない: text={cm_in_text} margin={mc_set}")

    # underline_word が _b.en に実在するか
    for para in p.get("paragraphs", []):
        for s in para:
            if "underline_word" in s:
                if s["underline_word"] not in s["en"]:
                    bad.append(f"大問4: {s['id']}.underline_word '{s['underline_word']}' が en の中に無い: '{s['en']}'")
                if s["underline_word"].endswith("."):
                    bad.append(f"大問4: {s['id']}.underline_word の末尾にピリオド（一致しなくなる）")

    # teacher_comment が passages[0] 直下に存在
    tc = p.get("teacher_comment")
    if not (isinstance(tc, dict) and tc.get("en") and tc.get("ja")):
        bad.append("大問4: teacher_comment が欠落（passages[0] 直下に {en, ja} 必須）")

    return bad
```

#### F. round-01 を別ラウンドへ移植する具体手順（最短経路）

1. `tools/build_readlens_2026_round01.py` の **`SECTION_4` ブロック全体（行 1239〜1666）をコピー**して `build_readlens_2026_roundNN.py` の同位置に貼る
2. `passage.title.{en,ja}` を新ラウンドの草稿タイトルに差し替える
3. `paragraphs` の各文の `en` と `ja` を新ラウンドの本文・全訳に差し替える（**段落数・文の分割数は PDF と一致させる**。1文増えた／減ったときは id サフィックス連番を必ず引き直す）
4. `comment_marker` `marker_position` `marker_type` `underline_word` の **位置だけ** を PDF のキャレット・下線の位置に合わせて差し替える（フィールド名・値の書式は触らない）
5. `margin_comments` の `marker / en / ja` を新ラウンドの右カラムコメントに差し替え（**marker は文中と完全一致**）
6. `teacher_comment.{en,ja}` を新ラウンドの Overall Comments に差し替え
7. `questions[]` の英文・選択肢・正解・`evidence_sentences`・`instructor_note` を新ラウンドのものに差し替え（§4.2.1 のルール厳守）
8. `python tools/build_readlens_2026_roundNN.py` を走らせ、§3.4.1 E の検証スクリプトを実行 → 全て緑になるまでループ
9. ブラウザで §3.4.1 D の検証手順 E の目視チェック5項目を確認

### 3.5 大問5（配点16・3〜4問）— 実用文（メール往復＋スケジュール表＋配置図）

- **本文**:
  - 受信メール 1通＋送信メール 1通（往復形式）
  - 表（スケジュール）と図（会場配置）が**メール本文の途中**に挿入される
- **設問**: 表・図と本文の照合系・矛盾発見系・最適配置の選択問題（画像で答える4択もあり）。
- **JSON 上の特徴**:
  - 各メールを別 passage（`from_who_email` のような id）にする
  - 表は passage に `table: { columns: [...], rows: [...] }` を持たせ、`after_paragraph: 3` のように挿入位置を指定
  - 配置図は問題側に `figure_image` で
  - **タイムスタンプや件名はメール表記そのまま** (`From K. Tanaka · August 1, 2025 · Subject: ...`)

### 3.6 大問6（配点12・4問・各3点）— 物語文（陸ガメ等）＋ デイビッドへの感想メール

- **本文**: 比較的長い物語（10段落超）＋末尾に「あなたが書いた感想メール」が続く。
- **設問の典型**:
  - 問1〜3: 4択
  - 問4: **「2つの空所に5つから2つ選ぶ・順不同」**（`unordered_slots` 必須）
- **JSON 上の特徴**:
  - 物語 passage の `block_separators: [1, 4, 5, 6]` で「段落○の後ろに区切り線を入れる」を指定（冊子のレイアウトを再現するため）
    - **【重要】** 以前は `block_separators` によって音声ファイルもブロック単位に結合されていましたが、パラグラフ単位で細かい再生を行うため、現在はこの指定があっても**音声は常にパラグラフごとに分割・生成**される仕様に変更されました。
  - 感想メールは別 passage として末尾に `id: "feedback_email"` で
  - 問4 では `choices_30`, `choices_31` の **同一選択肢を二度** 持たせ、`unordered_slots: [30, 31]` をマーク
  - `answer: {"30": "①", "31": "⑤"}`、`answer_note: "順不同・両方正解で3点"`

### 3.7 大問7（配点16・5問）— 記事読解 ＋ 発表の概要（プレゼン形式）

- **本文**: A4 2〜3ページの英語記事 + 末尾に「発表の概要」（=見出し付きの空欄入りスライドメモ）。
- **設問**: 「概要の空欄に何が入るか」を本文情報で埋める。最終問は表挿入や図表対応もある。
- **JSON 上の特徴**:
  - passage を 2 つに分ける: `id: "article"` と `id: "outline"`
  - `outline` 側は `presentation_outline: { ... }` という独自フィールドを持つ（ビューア側 `viewer.js:307` で対応）
  - 図表が選択肢になる問題は `figure_image` または `choice_grid_image`
  - **Round-01 では設問解説の OCR が壊れやすい**（「I設問解説I」「弱司」など）。`tools/_section07.py` に切り出し用の正規表現がまとまっている。新ラウンドでも同じ汚染が発生したら、そこから流用する。

### 3.8 大問8（配点は回により 17〜18・5問前後）— レポート構成：意見読解・立場・情報源活用

- **本文（典型）**: Step 1〜3 の指示文＋**複数筆者の意見**（Step 1）＋**エッセイアウトライン**（Step 3）＋**資料A**（論説）＋**資料B**（**図表・折れ線グラフ** 等）。
- **設問の典型**:
  - Step 1 読了後: 意見照合・imply 等（4択）
  - **Step 2**: 問題文で立場が与えられたうえで、**筆者コード複数空所＋主張の空所**（**全空所正解で得点**・**筆者2枠は順不同** の指示あり）
  - Step 3: 資料A/B に基づく要約選択（4択）
- **JSON 上の特徴（駿台 round-01 系）**:
  - **画像が問題冊子（英語軸）と解説冊子（日本語軸）で別ファイル**になることがある。
    - 例: `images/.../q08/problem_source_b_line_graph_en_axes.png` / `explain_source_b_line_graph_ja_axes.png`
  - スライドメモ型の回では `is_presentation: true` + `slides: [...]`（ビューア `viewer.js` のプレゼン表示）
  - ビルドは `uni-ent-exeam-engl/tools/_section08.py` の `build_section_8(QUOTED_SOURCE)` 等を経由
- **JSON 上の特徴（ReadLens-Pro / Z会 round05 大問8 実績）** — §8.1 と併読:
  - Step 1 意見: `passage.layout: "speaker_boxes"`。各段落は `[{ id: "*_h", en, ja }, { id: "*_s1", … }]` のように **見出し文（id が `_h` で終わる）＋本文**。
  - Step 3 アウトライン: `layout: "essay_outline_box"`。`outline_title` / `outline_subheader` / `outline_line` は文オブジェクトの **`role`** で区別。
  - 資料B のグラフ: `passage.graph_image: { "src": "images/....png", "after_paragraph": 1, "alt": "..." }` ※ **`after_paragraph` は 1 始まり**（`viewer.js` 実装に合わせる）。
  - 設問ブロックの **`position_box` / `pre_header` / `inline_solve_markers`** は駿台大問8と同型（§8.1）。
  - Step 2 の複合採点: `answer_numbers: [41,42,43]`, `unordered_slots: [41, 42]`, `choices_41` … `choices_43`, `answer: {"41":"③","42":"⑤","43":"③"}`, **`points_note`** で「すべて正解で得点」を明記。

---

## 4. データ転載の鉄則（最重要ポリシー）

> このプロジェクトは「学習者が解説冊子を Web で読み返す」用途であり、**塾の解説の一字一句が学習者の信頼を支えている**。以下のルールを破ると、教材としての価値が崩れる。  
> **他AIに委任する場合は §4.2.1（必須プロンプト）を必ず読むこと**。`instructor_note` を生成し忘れる事故の予防策がそこにまとまっている。

### 4.1 逐語転載が義務のフィールド

| フィールド | 出典 | 転載ポリシー |
|---|---|---|
| `passages[].title.en` / `paragraphs[][].en` | 問題冊子 | OCR崩れだけ清書（fbr→for, fb1→fb1ラインドロップ等）。**意味補正・要約は禁止** |
| `passages[].title.ja` / `paragraphs[][].ja` | 解説冊子の全訳 | 句読点も含め冊子どおり |
| `questions[].stem.en` / `choices[].en` | 問題冊子 | 同上 |
| `questions[].stem.ja` / `choices[].ja` | 解説冊子 | 同上 |
| `questions[].explanation.quoted_ja` | **解説冊子の「設問解説」** | ★最重要。改行・連結記号も冊子に従う |
| `questions[].explanation.quoted_source` | 固定 | `"駿台『2026年駿台実戦問題集 英語リーディング』第N回 解説冊子"` |
| `vocabulary[].items[].en` / `.ja` / `.note` | 解説冊子の「主な語句・表現」 | 同上 |

### 4.2 「学習者向け補足」は instructor_note のみに切り出す

冊子に書かれていない**ガイド・要約・推論・テクニック**はすべて以下の構造で `instructor_note` に閉じ込める。ビューア側はこのブロックだけ「グレーイタリック・角丸枠・"講師からの＋α"」として控えめに表示する仕様（`viewer.js:1659`）。

```json
"instructor_note": {
  "ja": "1〜2文の概要文（なぜ／どこを見るか）",
  "points": [
    "選択肢ごとの消し方／頻出表現／覚え方など、箇条書きで4〜6点",
    "他の設問との関連や、ダミー対策の具体的な切れ目"
  ]
}
```

**チェック**: もし冊子の `quoted_ja` の中身を抜粋・要約して `instructor_note` を書きそうになったら、それは「冊子の言葉を二度書いている」だけ。**新しい価値**（例: 「②と③が両方〇〇に見えるが、`p3_s2` の `who thought ...` で関係詞節が因果を担っているのが決定打」など、文IDを使った具体的な指差し）が無ければ点数化しない。

---

### 4.2.1 ★ instructor_note を必ず生成させる手法（他AI起用時の必須プロンプト）

> **背景**: 過去に他のAIエージェントへ委任した際、`instructor_note` が完全に欠落し、代わりに `quoted_ja` を別フィールドへコピーしただけの「他の選択肢の解説」が生成されてしまう事故があった（ラベル指定漏れによりビューア上で `undefined 〜` と表示される）。同じ事故を再発させないため、**他AIへ依頼する全プロンプトに本節のテンプレを必ず含めること**。

#### A. 失敗パターン（絶対に作らせてはいけない出力）

```jsonc
// ✗ NG例: instructor_note が無く、代わりに「他の選択肢の解説」を quoted_ja の重複で埋めている
"explanation": {
  "quoted_ja": "正解は④。『Whispers of the Past』のレビューの第1文…（中略）…『Stars Beyond Reach』の…(略)…③も誤り。",
  "quoted_source": "駿台『2026年駿台実戦問題集 英語リーディング』第2回 解説冊子",
  "other_choices_explanation": [   // ← こんなフィールドは存在しない
    "『The Final Clue』のレビューの第2文… ①は誤り。",
    "James R. Ellisonは…②は誤り。",
    "『Stars Beyond Reach』のレビューの第2文… ③も誤り。"
  ]
  // instructor_note: 完全欠落 ← ★ ここが事故
}
```

UI 上の症状: グレーの「講師からの＋α」枠が出ない／「他の選択肢の解説」のラベルが `undefined` になる／本文が解説冊子の二度書きで冗長。

#### B. 成功パターン（this を生成させる）

```jsonc
"explanation": {
  "quoted_ja": "正解は④。『Whispers of the Past』のレビューの第1文 (Whispers of the …) で「やや変わった恋愛物語」と紹介されており、…（解説冊子そのまま、改行も冊子に従う）…",
  "quoted_source": "駿台『2026年駿台実戦問題集 英語リーディング』第2回 解説冊子",
  "evidence_sentences": ["wp_review_s1", "wp_review_s3", "tfc_review_s2", "sbr_review_s2"],
  "instructor_note": {                                      // ← ★ 必須
    "ja": "『〜は単なる○○ではない』型の設問は、3冊のレビューを横串で比較し『内容が一部しか書かれていない／触れられていない』選択肢を機械的に消すのが最速。",
    "points": [
      "①の罠: tfc_review_s2 で『Laura Hale という探偵が登場する』だけ書かれ、『多くの探偵たち』(many detectives) は本文に無い。『a + 単数』vs『複数』のすり替えは共通テスト頻出。",
      "②の罠: 著者 James R. Ellison がロンドン在住か否かは tfc_review に記述なし。『舞台＝ロンドン』と『著者の居住地＝ロンドン』を読み手が混同するように仕組まれている。",
      "③の罠: sbr_review_s2 は『宇宙へ冒険に出かける』止まりで、『宇宙について学ぶ』(learn about space) という学習要素は書かれていない。動詞句の意味のすり替え。",
      "④の決定打: wp_review_s1 の `not just a simple romance novel` と s3 の `full of hidden secrets` がペアで効いている。形容詞 `simple` を否定する `not just …` 構文を見落とすと正解できない。",
      "テクニック: レビュー比較問題は『主語(誰の話か)・動詞(何をするか)・目的語(何について)』の3点照合表を頭の中で作り、選択肢の各語をマス目に入れて埋まらないものを消す。"
    ]
  }
}
```

#### C. 他AIに渡す **必須プロンプト・テンプレート**（コピペ用）

> 大問単位で他AIに作業委任する際、以下のブロックを **そのままプロンプト末尾に必ず貼る**。一字も削らない。

```text
=== 必須出力ルール（絶対遵守。違反は全件やり直し）===

各 question.explanation には、以下 4 フィールドを **必ず全て** 含めること。
1 つでも欠けていたら不合格として作業を最初からやり直す。

(1) quoted_ja            : 解説冊子からの逐語転載のみ。要約・補足追加は禁止。
(2) quoted_source        : 固定文字列「駿台『2026年駿台実戦問題集 英語リーディング』第N回 解説冊子」
(3) evidence_sentences   : 解説冊子が「第N段落第M文」と明示している箇所の文IDを配列で列挙
(4) instructor_note      : 下記スキーマで必ず生成。空オブジェクト・null・省略はすべて NG。

instructor_note のスキーマ（厳守）:
{
  "ja": "<1〜2文の概要文。『なぜこの設問でここを見るのか』を学習者目線で>",
  "points": [
    "<箇条書き 4〜6点。各点は次のいずれかの形を取ること>",
    "  - 誤答選択肢ごとの『罠の構造』(主語すり替え／単複すり替え／時制ずれ／因果反転 等)",
    "  - 正解の決定打となる本文の構文・接続詞・関係詞節への指差し（必ず文IDを本文中に書く）",
    "  - 同じ大問内の他設問や別ラウンド頻出パターンとの関連",
    "  - 学習者が暗記すべき構文・語彙・解法フレーズ（『not just A but B』『be aimed at …』等）"
  ]
}

instructor_note は quoted_ja の言い換えであってはならない。
quoted_ja に書かれている文を縮めただけ／同じ語順で訳し直しただけ／同じ根拠を別表現で繰り返しただけ
の場合は『価値ゼロ』として全 points を破棄して書き直すこと。
新しい価値とは:
  ・冊子に書いていない『罠の名前付け』(例: 単複すり替え／因果反転)
  ・冊子に書いていない『他選択肢との横比較の3点照合表』
  ・冊子に書いていない『学習者の覚え方・暗記用フレーズ』
  ・冊子に書いていない『この型の設問への普遍的な解法手順』

禁止事項:
- explanation 配下に other_choices_explanation のような独自フィールドを新設すること（ビューア未対応・UI で undefined になる）
- quoted_ja を分割して複数フィールドに散らばせること（必ず 1 フィールドに収める）
- instructor_note を空配列・null・キー欠落で出すこと
- 講師の口調にすること（敬体に統一。「〜だぜ」「〜してね！」等は不可）

自己検証手順（提出前に必ず実行）:
1. すべての question について instructor_note.ja が 10 文字以上あることを確認
2. すべての question について instructor_note.points が 3 件以上あることを確認
3. 各 points の各文に、文ID（p1_s2, wc_s3 等）が最低 1 つは含まれていることを確認
   （含まれない場合は『冊子の言い換え』である可能性が高いので書き直し）
4. evidence_sentences の各IDが passages 内の文オブジェクトに実在することを確認
5. explanation 配下に上記 4 フィールド以外のキーが無いことを確認
=== ここまで ===
```

**補足（Z会・短文ブリーフ実装）**: `quoted_source` を Z会表記にしてよいことは §0.1 のとおり。`instructor_note.points` の**すべて**に文IDを入れるのが理想だが、誤答の「罠の型」だけを短く列挙する点は許容しうる。その場合は **少なくとも概要文 `ja` と半数以上の points に文ID** を入れ、プロジェクトの検証スクリプト側で閾値を調整する（§6.5 の自動検査と整合させる）。

#### D. 受け取り後の自動検証（人間／親エージェント側）

他AIから戻ってきた `data.json` は、必ず以下のミニ検査を流してから受領する。`§6.5` の検証スクリプトに以下のチェックを **追加** する:

```python
import json
from pathlib import Path

ALLOWED_EXPL_KEYS = {"quoted_ja", "quoted_source", "evidence_sentences", "instructor_note"}
data = json.loads(Path("../ReadLens-Pro/data/sundai/2026/round02/data.json").read_text(encoding="utf-8"))

bad: list[str] = []
for sec in data["sections"]:
    for q in sec.get("questions", []):
        ex = q.get("explanation") or {}
        tag = f"sec{sec['section_number']} {q['question_id']}"

        # (1) 余計なフィールド検出
        extra = set(ex.keys()) - ALLOWED_EXPL_KEYS
        if extra:
            bad.append(f"{tag}: 余計なフィールド {extra}（ビューア未対応・UIで undefined 化の原因）")

        # (2) instructor_note の存在と中身
        note = ex.get("instructor_note")
        if not isinstance(note, dict):
            bad.append(f"{tag}: instructor_note が dict ではない（欠落／null／配列）")
            continue
        if len(note.get("ja", "")) < 10:
            bad.append(f"{tag}: instructor_note.ja が短すぎる／空")
        if len(note.get("points", [])) < 3:
            bad.append(f"{tag}: instructor_note.points が3点未満")
        # (3) 各 point に文IDが含まれているか（冊子の言い換えチェック）
        import re
        sid_re = re.compile(r"\b[a-z]{1,4}\d?_s\d+\b|\bp\d+_s\d+\b")
        for i, p in enumerate(note.get("points", [])):
            if not sid_re.search(p):
                bad.append(f"{tag}: points[{i}] に文IDが含まれない（冊子の言い換えの疑い）: {p[:40]}…")

if bad:
    print("【受領 NG】以下を他AIに突き返して書き直し依頼:")
    for b in bad:
        print(" -", b)
    raise SystemExit(1)
print("【受領 OK】全 question で instructor_note 完備")
```

このスクリプトが赤になったまま `git commit` しないこと。  
赤になった項目は、§4.2.1 の **必須プロンプト・テンプレート（C）をそのまま貼って書き直しを依頼**する。

#### E. 委任プロンプトの全体ひな型（ヘッダ込み・コピペ用）

```text
あなたは駿台『2026年駿台実戦問題集 英語リーディング』のデータ整備を担当する作業者です。
担当: 第N回 大問X
入力: 
  - data/sundai-2026-reading/round-NN/sections/qXX.json （PDF抽出生データ）
  - 解説冊子 第N回 解説 (PDFのpXX-pYY)
出力先: 
  - tools/build_readlens_2026_roundNN.py の SECTION_X を実装
  - 生成後 python tools/build_readlens_2026_roundNN.py を実行して
    ReadLens-Pro/data/sundai/2026/roundNN/data.json に反映

参照すべき手引書: uni-ent-exeam-engl/問題実装手引書.md
特に必読セクション:
  §3.X (担当大問の典型構造)
  §4.1〜4.4 (転載ポリシー)
  §4.2.1 (instructor_note 生成ルール)  ← ★ 違反禁止
  §5 (JSONスキーマ)

[ここに §4.2.1 C の『必須出力ルール』ブロックを丸ごと貼る]

完成後、以下を本文に含めて報告すること:
1. 各 question の instructor_note.points の件数一覧
2. 各 question の instructor_note.points の各点に含まれる文ID一覧
3. evidence_sentences が passages 内に実在することの確認結果
```

### 4.3 evidence_sentences の正しい使い方

- 解説冊子が「第N段落第M文」と明示している箇所だけを文ID（例 `wc_s3`, `p3_s2`）で並べる
- 推測や「だいたいこの段落」では入れない（学習者がクリックしてジャンプしたときに正しく光る必要があるため）
- 文IDの命名は **passage.id ではなく、文オブジェクトの id**（既存例の慣行: `wc_s3` = winter_care section の3文目、`p6_s7` = paragraph 6 sentence 7）に従う

### 4.4 OCR 汚染の典型例（清書時の置換ルール）

| OCR 出力 | 正しい英語 | 備考 |
|---|---|---|
| `fbr` | `for` | f+b+r がリガチャ崩れ |
| `Hies` / `Hytraps` | `flies` / `flytraps` | fl リガチャ崩れ |
| `nytraps` | `flytraps` | 同上 |
| `Tbmperature` | `Temperature` | Te → Tb |
| `O`（独立アルファベットの O） | `①〜⑨ などの選択肢番号` | 文脈で判定 |
| `「冒一]` `「巨司」` `「弱司」` | 解答番号枠 | 解説冊子の `[N]` の OCR崩れ。ビルダーでは無視 |
| 縦組みの `ｌｉｉｉ`／`｜｜｜｜` | レイアウト罫線残骸 | `_section07.py:_strip_layout_noise` の方式で1行ごと判定して削除 |
| `『 』`（半角崩れ） | `「 」` | 全角に統一 |
| `（各 N 点）` のスペース崩れ | `（各N点）` | 採点ルール抽出時に正規表現で揃える |

> **作業手順**: OCR 崩れは `_q01_vocab_full.txt` のような中間ファイルに一旦書き出し、grep で全箇所を確認してからまとめて置換する。1箇所ずつ目で直すと必ず取りこぼす。

---

## 5. ReadLens-Pro `data.json` のスキーマ要点

> ビューア（`ReadLens-Pro/viewer.js`）が実際に参照するキーだけを抜粋した実用スキーマ。  
> 完全形は `ReadLens-Pro/data/sundai/2026/round01/data.json` を参照すること。

```jsonc
{
  "exam_info": {
    "title": "...",
    "publisher": "駿台",
    "year": 2026,
    "round": 1,
    "subject": "英語（リーディング）",
    "time_limit_minutes": 80,
    "total_answer_numbers": 47,
    "implemented_sections": [1,2,3,4,5,6,7,8],
    "source_pdf_mondai": "第1回_問題.pdf",
    "source_pdf_kaitou": "第1回_解説.pdf",
    "data_policy": "..."
  },
  "sections": [
    {
      "section_number": 1,
      "title": "第1問",
      "points": 6,
      "points_per_question": 2,        // 任意。これがあると「配点6点（各2点×3問）」と表示
      "description": "短文読解（パンフレット）",
      "situation": { "en": "...", "ja": "..." },

      "passages": [
        {
          "id": "header" | "section1_light" | "story" | "tanaka_email" | "article" | "outline" | ...,
          "title":    { "en": "...", "ja": "..." },
          "subtitle": { "en": "...", "ja": "..." },                    // 任意
          "image":    { "src": "data/.../images/q01_*.png", "alt": "...", "float": "right" }, // 任意
          "portrait_image": { "src": "...", "alt": "...", "float": "right" },                // 大問7用・任意
          "block_separators": [1, 4, 5, 6],                            // 任意・段落区切り
          "is_subsection_header": false,                               // 6AB分割時のみ
          "no_divider": false,                                          // 直前との区切り線抑制
          // 文の持ち方は2通り:
          "sentences": [ { "id": "wc_s3", "en": "...", "ja": "...", "role": "title" } ],
          "paragraphs": [
            [ { "id": "p1_s1", "en": "...", "ja": "..." }, ... ],
            [ { "id": "p2_s1", "en": "...", "ja": "...",
                "comment_marker": "(1)", "marker_position": "before", "marker_type": "caret" } ]
          ],
          // 大問5・8 等の特殊コンポーネント:
          "table": { "columns": [...], "rows": [...], "after_paragraph": 3 },
          "presentation_outline": { ... },                              // 大問7
          "is_presentation": true, "slides": [ { "title": {...}, "columns": [...] } ] // 大問8
        }
      ],

      "questions": [
        {
          "question_id": "問1",
          "answer_number": 1,
          "answer_numbers": [30, 31],                                   // 複数空所のとき
          "unordered_slots": [30, 31],                                  // 順不同のとき
          "stem": { "en": "...[1]...", "ja": "...[1]..." },
          "figure_image": { "src": "...", "alt": "...", "caption_ja": "...", "caption_en": "..." }, // 絵で答える設問（キャプション任意・英日両方可）
          "choice_grid_image": { ... },                                  // 任意
          "choices": [
            { "label": "①", "en": "...", "ja": "...", "is_correct": true },
            ...
          ],
          // 順不同や複数空所では:
          "choices_30": [ ... ], "choices_31": [ ... ],
          "answer": "①" | { "30": "①", "31": "⑤" },
          "answer_note": "全部正解で3点" | "順不同・両方正解で3点",     // 任意
          "explanation": {
            "quoted_ja": "正解は①。〜（解説冊子の逐語）〜",
            "quoted_source": "駿台『2026年駿台実戦問題集 英語リーディング』第N回 解説冊子",
            "evidence_sentences": ["wc_s3"],
            "instructor_note": {
              "ja": "1〜2文の概要",
              "points": ["箇条書き4〜6点"]
            }
          }
        }
      ],

      "vocabulary": {
        "lead_text": { "label_ja": "リード文・タイトル", "items": [ {"en": "...", "ja": "...", "note": "..."} ] },
        "p1": { "label_ja": "第1段落 (...)", "items": [ ... ] },
        "p2": { ... }, "feedback_email": { ... }, "passage_outline": { ... }, "question_kw": { ... }
      }
    }
  ]
}
```

---

## 6. パイプラインの実行手順

### 6.1 Step (a): PDF からの一次抽出

```powershell
# uni-ent-exeam-engl/ で
python tools/extract_sundai_round.py
```

これで `data/sundai-2026-reading/round-01/sections/q01.json 〜 q08.json` と `manifest.json` が再生成される。**OCR崩れを含む生テキストがそのまま入る**ので、ここで「fbr→for」などには手を出さない（次工程の `build_*.py` 内で意味のある単位で清書する）。

### 6.2 Step (b)(c): 図版の切り出しと登録

1. PDF を Cursor 等のビューアで開き、必要な図版をクリック → 「画像として保存」  
   → Cursor の場合、画像は  
   `C:\Users\makoto\.cursor\projects\c-Users-makoto-Documents-GitHub-uni-ent-exeam-engl\assets\image-<uuid>.png` に保存される。
2. `tools/register_qNN_assets.py`（大問1・2用は既存。3以降は同じ書式で複製）を編集し、`FIGURES` 配列に各図版の `id`, `phase`, `kind`, `file`, `altJa`, `hintForImport` を追記。
3. 実行:
   ```powershell
   python tools/register_q01_assets.py --copy
   ```
   `--copy` を付けると `hintForImport` の UUID で Cursor assets を `images/sundai-2026-reading/round-01/qNN/` に自動コピー。

### 6.3 Step (d): SECTION_X の執筆

`tools/build_readlens_2026_round01.py` の中の `SECTION_1`〜`SECTION_6` を埋める。大問7・8 は別ファイル `tools/_section07.py`, `tools/_section08.py` の `build_section_7/8(QUOTED_SOURCE)` を更新。  
**書く順序の推奨**:

1. `situation` → `passages`（本文と全訳を文ID付きで対応）
2. `questions[i].stem` → `choices`（正解だけ `is_correct: true`）
3. `questions[i].explanation.quoted_ja` を解説冊子から逐語転載（OCR崩れだけ清書）
4. `evidence_sentences` を解説冊子の指示どおり拾う
5. **最後に** `instructor_note.ja / points` を、冊子に書いていない学習価値だけ拾って書く
6. 大問末の `vocabulary` を「主な語句・表現」セクションから整える

### 6.4 Step (e): ビルド & ReadLens-Pro へ転写

```powershell
python tools/build_readlens_2026_round01.py
```

これで以下が走る:

1. `images/sundai-2026-reading/round-01/qNN/*.png` を `ReadLens-Pro/data/sundai/2026/round01/images/` に **既定の命名規則**（`qNN_<元ファイル名>.png`）でコピー（マッピングは同スクリプト冒頭 `IMAGE_COPY_MAP`）
2. `data.json` を `ReadLens-Pro/data/sundai/2026/round01/data.json` に書き出し

ビルド出力は `_build_log.txt`〜 `_build_logN.txt` のように追記して保存しておく（履歴 grep に便利）。

### 6.5 Step (f): 検証

`_verify*.txt` の生成例（既存の `_verify4.txt` を参照）。最低限以下をチェックするミニスクリプトを書いて、出力をログ化する:

- 各 question について `answer` フィールドの存在
- `instructor_note.ja` の存在と `points.length >= 3`
- `evidence_sentences[*]` が `passages` に実在する文IDであること
- `choices[].is_correct: true` が `answer` と一致

ミニスクリプト雛形:

```python
import json, sys
from pathlib import Path
data = json.loads(Path("../ReadLens-Pro/data/sundai/2026/round01/data.json").read_text(encoding="utf-8"))
all_ids: set[str] = set()
for sec in data["sections"]:
    for p in sec.get("passages", []):
        for s in p.get("sentences", []) or []:
            all_ids.add(s["id"])
        for para in p.get("paragraphs", []) or []:
            for s in para:
                all_ids.add(s["id"])
for sec in data["sections"]:
    for q in sec.get("questions", []):
        ex = q.get("explanation", {})
        evid_bad = [e for e in ex.get("evidence_sentences", []) if e not in all_ids]
        ans = q.get("answer")
        note_intro = bool(ex.get("instructor_note", {}).get("ja"))
        note_points = len(ex.get("instructor_note", {}).get("points", []))
        print("sec", sec["section_number"], q["question_id"],
              "ans=", ans, "note_intro=", note_intro,
              "note_points=", note_points, "evidence_bad=", evid_bad)
```

### 6.6 Step (g): ビューア起動

```powershell
cd C:\Users\makoto\Documents\GitHub\ReadLens-Pro
.\start-server.ps1
# → http://localhost:8000/viewer.html?data=data/sundai/2026/round01/data.json
```

### 6.7 Z会 `data/zkai/` のビルド（ReadLens-Pro 内）

大問ごとに `build_sectionMM.py` を置き、当該ラウンドのディレクトリで実行する。

```powershell
cd C:\Users\makoto\Documents\GitHub\ReadLens-Pro\data\zkai\2026\round05
python build_section08.py   # 例: 第8問のみ差し替えマージ
python build_section07.py   # 例: 第7問（グラフ再生成を含む場合あり）
```

- 生成物は **同階層の `data.json`** と **`images/`**。
- ビューア例: `viewer.html?data=data/zkai/2026/round05/data.json`（パスはデプロイ構成に合わせる）。

上記 **§6.6** でサーバを起動したうえで、ブラウザで開き、**全大問について以下を目視確認**:

1. 本文の全訳トグル・evidence ジャンプが動く
2. 図版が崩れず表示される
3. 設問の正解ハイライト・解説のグレー枠（instructor_note）が分離されている
4. 順序整列・順不同・複数空所が壊れていない
5. ページ送り・大問切替が動く

---

## 7. AI エージェント間の引き継ぎプロトコル

> このプロジェクトは複数の AI が時間差で着手する。「あなたが今書いた知見が、3時間後に別 AI が見て同じ判断ができる状態」を保つこと。

### 7.0 ★ 他AIへ作業を委任する場合の必須手順

別の AI エージェント（外部 LLM・サブエージェント等）に大問単位で作業を渡すときは、**必ず以下の3点セットを守る**。1つでも省略した結果、`instructor_note` が抜け落ちた `data.json` が混入する事故が報告されている（§4.2.1 A 参照）。

1. **プロンプトに §4.2.1 C「必須出力ルール」を一字一句そのまま貼る**（要約・短縮禁止）
2. 受領後、**§4.2.1 D の検証スクリプトを実行**してから受け入れる（赤が1つでも残っている間はマージしない）
3. 赤を返すときは、**§4.2.1 C のブロックをもう一度貼って**書き直しを依頼する（口頭で「直して」だけだと再発する）

委任プロンプトの全体雛形は §4.2.1 E にある。新規ラウンド着手時はこのテンプレートをコピーしてヘッダ部だけ書き換えるのが最短。

**大問4 を委任する場合のみ**、上記 §4.2.1 C に加えて **§3.4.1 D「大問4 厳密レイアウト再現プロンプト」も必ず併記**すること（レイアウト崩壊事故が過去最頻出のため）。受領後の検証も §4.2.1 D ＋ §3.4.1 E の両方を回す。

### 7.1 コミット境界の原則

- **大問1つ＝1コミット** を厳守。`build_*.py` の SECTION_X 1ブロックの編集が終わるたびに、`data.json` を再生成して `_verify*.txt` を更新し、3点セット（`SECTION_X`差分・`data.json`差分・verify ログ）でコミット。
- コミットメッセージの形式（必ず日本語）:
  ```
  round-01 大問N: <短い要約>
  
  - quoted_ja を解説冊子から逐語転載（OCR崩れ <例> を清書）
  - instructor_note: <主要なテクニック> を追記
  - evidence_sentences: <文ID...>
  - 既知の残課題: <あれば>
  ```

### 7.2 引き継ぎメモの場所

- ラウンド/大問レベルの未解決事項は `_handoff_round-NN_qXX.md`（ラウンドルートに置く）。書式:
  ```markdown
  ## 状態
  - [x] 本文・全訳
  - [x] 設問・選択肢
  - [ ] quoted_ja（解説冊子 p.21 の最終段落のみ未転載：OCR が荒く目視で起こす必要）
  - [ ] instructor_note: 問4 のみ未着手
  
  ## 次の作業者へ
  - PDF 21 ページ右下の図表は **解説冊子側にしか存在しない**。
    `images/.../qNN/explain_*.png` で別ファイルとして保存し、figure_image 側は問題冊子版だけを使うこと。
  - ...
  ```
- リポジトリ全体の方針変更は本手引書（`問題実装手引書.md`）に直接 PR で。

### 7.3 「あなたが暗黙にやっていること」を明文化するためのチェック

新しい AI が困りそうなポイントを残さないよう、以下を**必ず明示する**:

| 暗黙知 | 明示する場所 |
|---|---|
| 「fbr は for の OCR崩れ」 | この手引書 §4.4 |
| 「解説冊子の縦罫線 ｌｉｉ は無視」 | `tools/_section07.py:_strip_layout_noise` の docstring |
| 「画像は元サイズのまま、リサイズしない」 | `IMAGE_COPY_MAP` の上にコメントで |
| 「設問の点数が `(各3点)` か `(全部正解で3点)` か」 | `answer_note` で記録 |
| 「evidence_sentences の選び方」 | この手引書 §4.3 |
| 「instructor_note の長さ目安」 | この手引書 §4.2（4〜6点） |
| 「ビューアの新フィールド対応の有無」 | 本手引書 §5 と `viewer.js` の grep（"presentation_outline" 等） |

### 7.4 不確かなときの判断順位

1. **原典PDF**（駿台『2026年駿台実戦問題集 英語リーディング』第N回）
2. **既存の round-01 実装**（同型大問の `SECTION_X`）
3. **本手引書**
4. **ビューアの実装**（`ReadLens-Pro/viewer.js` で実際に使われているか）

これらが矛盾する場合は、**勝手に直さず** `_handoff_round-NN_qXX.md` に記録して人間に判断を仰ぐ。  
特に「ビューアが対応していないフィールドを新設する」は禁止（黙って data だけ追加されると無表示になり、検証時に見落とされやすい）。

---

## 8. よくあるトラブルと対処

| 症状 | 原因 | 対処 |
|---|---|---|
| `data.json` のビルドは通るが画像が出ない | `IMAGE_COPY_MAP` のキーと `images/.../qNN/` の実ファイルが食い違う | `_build_log*.txt` の `[warn] missing source image:` を grep |
| 設問の正解ハイライトが効かない | `choices[].is_correct` の真値と `answer` が不一致 | §6.5 の検証スクリプトで `is_correct` と `answer` の照合を必ず行う |
| 「evidence ジャンプ」しても本文が光らない | `evidence_sentences` の文IDが本文の文オブジェクトに存在しない | §6.5 の `evidence_bad` をログで監視 |
| 順序整列の正答が表示されない | `answer_numbers` と `answer` のキーがずれている／`unordered_slots` の指定漏れ | round-01 大問3・大問6 の構造を真似る |
| 大問7・8 のレイアウトが崩れる | `is_presentation`, `presentation_outline`, `block_separators` 等の特殊フィールドの記述漏れ | `viewer.js` の対応箇所（§5の grep）と既存 round-01 の SECTION を照合 |
| 大問8 問3 の立場メモ枠が表示されない | `position_box` フィールドが未定義、または stem に埋め込んだまま | 下記 §8.1 参照 |
| 大問8 の図が本文の想定位置に出ない | `graph_image.after_paragraph` の **1 始まり** と段落数のズレ | `viewer.js` の `pi + 1` 比較に合わせて再カウント |
| 4分割グラフと①〜④が対応しない | 画像にラベルが無い／キャプションが無い | §0.1・`build_section07.py`（パネル左上に①〜④＋`figure_image.caption_*`） |
| 「主な語句・表現」が一部抜ける | `tools/build_sundai_vocab_json.py` の `VOCAB_HEADER_RE` が認識しない見出し変種 | `_vocab_all_heads*.txt` を確認 → 正規表現を拡張 |
| OCR 解説の「I設問解説I」が見つからない | 罫線崩れによる別の表記（「Ⅰ設問解説Ⅰ」「设问解说」等） | round-01 同様、`_sectionXX.py` の `find()` マーカーを差し替える |

### 8.1 大問8 専用フィールド チェックリスト

駿台第8問（エッセイ準備型：Step1〜3 構成）は、他大問にない**専用フィールド**を持つ。実装時は以下を必ず揃えること。

#### ① `position_box`（問3 に必須）

問題冊子の Step 2 に登場する「立場メモ枠」（枠線で囲まれたボックス）を再現するフィールド。  
**viewer.js が `position_box` を専用の枠線ボックスとしてレンダリングするため、stem に埋め込んではいけない。**

```python
"position_box": {
    "en": (
        "Your position: <立場文>.\n"
        "  • Authors [41] and [42] support your position.\n"
        "  • The main argument of the two authors: [43]"
    ),
    "ja": (
        "あなたの立場：<立場文の日本語訳>。\n"
        "  • 筆者 [41] と [42] はあなたの立場を支持する。\n"
        "  • この2人の筆者の主な論拠： [43]"
    ),
},
```

> **注意**: 解答番号の枠（例: `[40][41][42]` か `[41][42][43]` か）は**回・出版社ごとに異なる**。ラウンドによって英文表現も微妙に変わる（"The main argument of the two authors:" / "An argument shared by the two authors:" 等）。**必ず問題冊子の原文と番号に忠実に転記**する。

**各回の立場文（en）実績:**

| ラウンド | 立場文（en） |
|---|---|
| Round 1 | `POSITION: Electric cars are a good idea.` |
| Round 2 | `Your position: Companies should encourage their employees to telework.` |
| Round 3 | `Your position: Teachers should keep a good balance between group work and individual work.` |
| Round 4 | `Your position: Schools should continue to allow morning practice for athletic clubs.` |
| Z会 round05 大問8（例） | `Your position: Testing medicine on animals is the right thing to do.`（動物実験・医薬） |

#### ② `pre_header`（問3 に必須）

設問パネルの問3 の上に "[Step 2] Take a position ..." の見出しを挿入するフィールド。

```python
"pre_header": {
    "en": "[Step 2] Take a position on <トピック>.",
    "ja": "［ステップ2］<トピックの日本語訳>について立場を決める。",
},
```

#### ③ `inline_solve_markers`（大問8 の各 passage に付ける）

学習者に「ここで設問を解く」「ここで本文に戻る」を案内するマーカー。  
**大問8 にのみ追加する。他の大問（大問6・7 など）には絶対に付けない。**

- `after_paragraph` は **0 始まりの段落インデックス**（`viewer.js` の `inline_solve_markers` 分岐）。`graph_image.after_paragraph` とは数え方が異なるので混同しない。

#### ④ 資料 passage の `graph_image`（折れ線・棒グラフ等）

資料Bのように本文の直後に図を差し込む場合、**その passage** に以下を付ける。

```jsonc
"graph_image": {
  "src": "images/section08_source_b_graph.png",
  "after_paragraph": 1,
  "alt": "Number of Animals Used in Medical Research"
}
```

- **`after_paragraph` は 1 始まり**（`pi + 1` と比較される。`inline_solve_markers` と逆なので要注意）。
- 画像は `matplotlib` 等で生成し、`data.json` と同じ `roundNN/` 配下の相対パスで参照する運用が多い（Z会 `build_section07.py` / `build_section08.py` 参照）。

#### ⑤ Step 2 複数空所の採点（`unordered_slots` / `points_note`）

問題文で立場が固定されているタイプでは、**筆者2枠＋主張1枠**のように空所が複数ある。

- `answer_numbers: [41, 42, 43]` のように列挙。
- 筆者コードなど **順不同の2枠** がある場合は `unordered_slots: [41, 42]` を必ず付与（ビューアが集合比較で正誤判定する）。
- `answer: {"41": "③", "42": "⑤", "43": "③"}` のように **キーは番号の文字列**。
- `choices_41`, `choices_42`, … のように **空所番号ごとに別配列**（駿台大問8・Z会大問8と同型）。
- **「すべて正解で得点」** 等は `points_note` と設問 `stem` の両方に明記すると学習者が見落としにくい。

#### ⑥ `question_image` / `position_box` の未表示に気づく手順

1. ブラウザで問3 を開く  
2. stem の直下（選択肢の上）に枠線ボックスが表示されているか確認  
3. 表示されない場合は `viewer.js` で `position_box` のレンダリングコードが存在するか grep する  
   ```
   grep "position_box" viewer.js
   ```
4. コードが無ければ `viewer.js` に追加（§7.4 の「ビューアが対応していないフィールド」ルールに従い PR を立てる）

---

## 9. 1ラウンド実装の最終 Done 定義

以下がすべて満たされて初めて「ラウンド N 完了」とする:

- [ ] 全8大問について `SECTION_N` または `_sectionNN.py` が完成（**Z会ライン**の場合は `ReadLens-Pro/data/zkai/2026/roundNN/build_sectionMM.py` が相当）
- [ ] `data/sundai-2026-reading/round-NN/sections/q*.json` の `figures` が全て埋まり、`images/sundai-2026-reading/round-NN/qXX/` に対応 PNG が存在
- [ ] `python tools/build_readlens_2026_roundNN.py` が `[warn]` なしで完走
- [ ] `_verify*.txt` で:
  - すべての question に `ans`（正解番号）が存在
  - すべての question に `instructor_note.ja`（10文字以上）と 3点以上の `points` が存在
  - `evidence_bad == []`（壊れた文IDが無い）
  - `explanation` 配下に §4.2.1 で許可された 4 フィールド以外のキーが無い（`other_choices_explanation` 等の独自フィールドの混入禁止）
  - `instructor_note.points` の各点に文ID（`p1_s2` 等）が最低1つ含まれる（＝冊子の言い換えになっていない）
- [ ] **大問4 のみ**: §3.4.1 E の `verify_section4(sec)` を実行して空配列（エラー無し）を確認、かつブラウザでの目視チェック5項目（§3.4.1 D の検証手順 E）すべて OK
- [ ] ビューア（`http://localhost:8000/viewer.html?data=data/sundai/2026/roundNN/data.json`）で全大問を目視確認
- [ ] `_handoff_round-NN_*.md` に未解決事項が無い、または全て次回引き継ぎが書かれている
- [ ] `data.json` ・ `images/` ・ verify ログ ・ 本手引書（変更がある場合）を1ラウンド分まとめてコミット

---

## 10. 参考: 用語のミニ辞書

| 略語・キー | 意味 |
|---|---|
| **大問 / section** | 「第1問」〜「第8問」のこと（共通テスト型） |
| **小問 / question** | 大問内の「問1」〜「問N」 |
| **配点 / points_per_question** | 例「配点6点（各2点×3問）」 |
| **passage** | 本文ブロック。物語1つ・メール1通・記事1本などが該当 |
| **figure_image** | 設問の選択肢が画像のときに使う |
| **evidence_sentences** | 解説冊子が「第N段落第M文」と指している文の文ID |
| **instructor_note** | 冊子に書かれていない、学習者向けの＋αメモ。`points` は箇条書き |
| **quoted_ja** | 解説冊子からの逐語転載部分（解答根拠の本文） |
| **block_separators** | 物語型 passage で「段落の後ろに区切り線を入れる位置」 |
| **position_box** | 大問8 問3 の「立場メモ枠」（Step 2 の枠線ボックス）。stem に埋め込まず専用フィールドに分離する。§8.1 参照 |
| **pre_header** | 設問パネルで問の直上に差し込む大見出し（例: "[Step 2] Take a position..."）。大問8 問3 に必須 |
| **question_image** | 設問パネル内に表示する画像（表・図など）。`imgBase` 相対パスで指定 |
| **graph_image** | passage 内に挿入する図。`after_paragraph` は **1 始まり**（`inline_solve_markers` の段落インデックスとは別） |
| **speaker_boxes** | 大問8 Step1 等。各段落を意見ボックス表示。見出し文の `id` は `_h` で終える慣例 |
| **essay_outline_box** | 大問8 Step3 のアウトライン枠。`role: outline_title` 等で行種別を指定 |
| **caption_en / caption_ja** | `figure_image` 直下の図の補足（選択肢とパネルの対応など）。`viewer.js` が両方表示可能 |
| **unordered_slots** | 順不同で複数空所に入る設問（大問6 問4、大問8 Step2 の筆者2枠など） |
| **points_note** | 複数空所の得点条件（例: ［41］〜［43］すべて正解で得点）を設問オブジェクトに明記 |
| **OCR 崩れ** | PDF 抽出時に `for` が `fbr` になる等のフォント由来エラー |

---

**最後に**: この手引書自体が「あなたの作業中に発見した暗黙知」を取りこぼしている可能性があります。新しい知見が出たら、勝手に PR を立てて本手引書を更新してください。**手引書のバージョンが現実より遅れることが、AI 共同作業で最も品質を落とす原因です**。

## 7. 音声データの生成

> **背景**: Webビューア上でのリスニング・音読支援のため、各段落の英文を `edge-tts` を用いて音声ファイル (.mp3) に変換し、提供します。

- **自動生成ツール**: `generate_audio_all.py` を使用します。
  - 実行例: `python generate_audio_all.py data/kyotsu/2025/honshiken/data.json --section 1`
- **クリーンアップ**: 読み上げ前に、英文データ内の `\n` や `\r` などの改行コード、および `<em>` などのHTMLタグは自動的に除去される仕様です。このため JSON 側の英文にこれらの記号が含まれていても不要な音声が混入することはありません。
- **除外ルール**: 見出しやタイトル（`role` が `pamphlet_heading`, `title`, `heading`, `outline_title`, `outline_subheader` 等）は、音声として連続して読まれると不自然なため、**読み上げから除外**するようにスクリプトが構成されています。
- **配置先**: JSON ファイルと同じディレクトリ内の `audio/` フォルダに出力されます。ビューアは JSON の構造と ID に基づいて自動的に音声を読み込みます。JSONファイル自体への追記は不要です。
